import React from 'react'
import './BlogCard.css'
const BlogCard = ({ name, title, author, authorImg, datePosted, topic }) => {
  return (
    <div className="Blo">
      <img />
      <h4>{name}</h4>
      <p>
        <span></span>
        <li></li>
      </p>
    </div>
  )
}

export default BlogCard
